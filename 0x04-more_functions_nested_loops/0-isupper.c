#include "main.h"
/**
*isupper-function check if charachter is upper or is no
*
*@c: is the input
*
*Return: function return 1 if is upper 0 otherwise
*/
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
